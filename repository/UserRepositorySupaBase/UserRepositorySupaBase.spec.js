import { describe, expect, it } from 'vitest'
// import { UserRepositorySupaBase } from './UserRepositorySupaBase'

describe('UserRepositorySupaBase', () => {
  it('create should throw if email, name or photoUrl is not provided', () => {
    // @ts-ignore
    // expect(() => new UserRepositorySupaBase().create())
    //   .rejects
    //   .toThrow('CreateUserRepositoryI param is required')
  })
})
